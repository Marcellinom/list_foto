package main

import (
	"fmt"
)

func main() {
	// old_infra := flag.String("old", ``, "json of old infrastructure metadata")
	// new_infra := flag.String("new", ``, "json of new infrastructure metadata")
	
	// flag.Parse()

	// var tes map[string]any
	// err := json.Unmarshal([]byte(*old_infra), &tes)
	// if err != nil {
	// 	fmt.Printf("err: %v\n", err.Error())
	// }
	fmt.Println("nothing to migrate...")
}